apiVersion: v1
kind: Namespace
metadata:
  name: stage
  labels:
    name: stage-label
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1
  namespace: stage
spec:
  selector:
    matchLabels:
      app: app1
  replicas: 2
  template:
    metadata:
      labels:
        app: app1
    spec:
      containers:
      - name: app1container
        image: "romankachur/node.js.image:43"
        resources:
          limits:
            cpu: "350m"
            memory: "128Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: app1-service
  namespace: stage
  labels:
    app: svc-1
spec:
  type: ClusterIP
  selector:
    app: app1
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  namespace: stage
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: app1.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app1-service
                port: 
                  number: 80


















